kind summary
title Installer comments and prerelease guidance aligned with release policy
edge applies-to blob:b5e1791eae137a4b8fc8750409f6f866c6cf02e0
edge targets-path path:install.sh

The installer comments now show correct examples for:
- latest integrated install from `main`
- pinned stable install via `VERSION=X.Y.Z`
- pinned prerelease install via `VERSION=X.Y.Z-rc.N`

Also fixed the warning text so it no longer implies that `main` is a stable release channel.
